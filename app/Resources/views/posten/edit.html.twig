{% extends 'base.html.twig' %}

{% block body %}
    <h1>Posten editieren</h1>

    {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
        <input type="submit" value="Edit" class="btn btn-warning btn-block" style="margin-bottom: 6px;"/>
    {{ form_end(edit_form) }}

    
            <a href="{{ path('posten_index') }}">Back to the list</a>
        
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete" class="btn btn-block btn-danger" style="margin-top: 6px;">
            {{ form_end(delete_form) }}
       
{% endblock %}
