{% extends 'base.html.twig' %}

{% block body %}
    <div class="box">
        <div class="box-header with-border">
            <h1 class="box-title" >Kunde creation</h1>
        </div>
        <div class="box-body">
            {{ form_start(form) }}
            {{ form_widget(form) }}
            <input type="submit" value="Create" class="btn btn-block btn-success" style="margin-bottom: 6px;"/>
            {{ form_end(form) }}

            
                    <a href="{{ path('kunde_index') }}" class="btn btn-block btn-info">Back to the list</a>
                <
        </div>
    </div>
{% endblock %}
